The user approved this resource. Write one wiki page to resources/ using the wiki.write MCP tool. Frontmatter must follow the schema in .ire/_SYSTEM.md: `title` (human-readable resource title), `type: summary`, `sources: [<all original sources shown in the prior turn, in order>]`, `updated: YYYY-MM-DD` (today), and `summary` (one-line summary). Start the body with a # heading matching the title, then the summary from your previous response.
