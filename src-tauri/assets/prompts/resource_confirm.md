The user approved this resource. Write one wiki page to resources/ using the wiki.write MCP tool. Frontmatter must follow .ire/wiki/_schema.md: `title` (human-readable resource title), `type: summary`, `sources: [<all original sources shown in the prior turn, in order>]`, `updated: YYYY-MM-DD` (today). Start the body with a # heading matching the title, then the summary from your previous response.
